query lifecycleAttachmentByTypeAndIdentifier($type: LifecycleAttachmentType!, $identifier: String!) {
    LifecycleAttachment(type: [$type], identifier: [$identifier]) {
        id
        body
        name
        skill
        ts
    }
}
