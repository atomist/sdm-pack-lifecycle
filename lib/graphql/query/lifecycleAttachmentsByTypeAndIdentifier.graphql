query lifecycleAttachmentsByTypeAndIdentifier($type: LifecycleAttachmentType!, $identifier: String!) {
    LifecycleAttachment(type: [$type], identifier: [$identifier]) {
        id
        body
        name
        skill
        configuration
        ts
    }
}
