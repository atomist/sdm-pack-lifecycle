query lifecycleAttachmentsByTypeAndIdentifier($identifier: String!) {
    LifecycleAttachment(identifier: [$identifier]) {
        id
        body
        name
        skill
        configuration
        ts
    }
}
