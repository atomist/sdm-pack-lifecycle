mutation kickUserFromSlackChannel(
  $teamId: String!
  $channelId: String!
  $userId: String!
) {
  kickUserFromSlackChannel(
    chatTeamId: $teamId
    channelId: $channelId
    userId: $userId
  ) {
    id
  }
}
