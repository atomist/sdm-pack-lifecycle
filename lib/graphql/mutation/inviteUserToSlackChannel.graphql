mutation inviteUserToSlackChannel(
  $teamId: String!
  $channelId: String!
  $userId: String!
) {
  inviteUserToSlackChannel(
    chatTeamId: $teamId
    channelId: $channelId
    userId: $userId
  ) {
    id
  }
}
