subscription UpdateOnJob {
    AtmJob {
        id
        data
        name
        owner
        state
        createdAt
        updatedAt
        completedAt
        jobCount
        completedCount
        jobTasks {
            state
            message
        }
    }
}
